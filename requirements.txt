# D-FINE Label Studio ML Backend (cdfine-almond-backend) - Requirements
# Minimum Python Version: 3.8+

# Core Deep Learning (Required)
torch>=2.0.0
torchvision>=0.15.0

# Image Processing (Required)
Pillow>=9.0.0

# Numerical Computing (Required)
numpy>=1.21.0

# AWS S3 Access (Required - for loading images from S3)
boto3>=1.26.0

# Label Studio ML SDK (Required - ML backend framework)
label-studio-ml>=1.0.9

# Visualization (Used in object_detection_pipeline.py)
matplotlib>=3.5.0

# ============================================================================
# GPU SUPPORT (Optional but Recommended for Speed)
# ============================================================================

# For NVIDIA GPU (CUDA):
# Uninstall CPU version first: pip uninstall torch torchvision
# Then install CUDA version from: https://pytorch.org/get-started/locally/
#
# Example for CUDA 11.8:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#
# Example for CUDA 12.1:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# For Mac with Apple Silicon (M1/M2/M3):
# PyTorch with MPS (Metal Performance Shaders) support is included in torch>=2.0.0
# No additional installation needed - MPS acceleration works automatically

# ============================================================================
# INSTALLATION INSTRUCTIONS
# ============================================================================

# Standard installation (CPU only):
#   pip install -r requirements.txt

# Check if GPU is available after installation:
#   python -c "import torch; print('CUDA:', torch.cuda.is_available()); print('MPS:', torch.backends.mps.is_available())"

# ============================================================================
# NOTES
# ============================================================================
# - All other dependencies (json, os, sys, argparse, pathlib, base64, io,
#   urllib, re, math, copy, functools, collections, logging, time) are Python standard library
# - The script automatically detects and uses GPU if available
# - CPU fallback is automatic if no GPU detected
